{
	"name": "@normie28/yahtzee-full",
	"author": "normie28",
	"version": "0.1.0",
	"description": "Normie's Yahtzee, a simple and light version of Yahtzee",
	"dependencies": {
	},
	"scripts": {
		"start": "cd server && npm start",
        "build": "cd server && npm install && cd ../client && npm install && npm run build"
	}
}
